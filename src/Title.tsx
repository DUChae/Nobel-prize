import "./Title.css"; // Title 컴포넌트의 CSS 파일을 임포트

const Title = () => {
  return (
    <div className="title-container">
      <h1>Discover Nobel Prize Winners</h1>
      <h2>Explore the achievements of laureates!</h2>
    </div>
  );
};

export default Title;
